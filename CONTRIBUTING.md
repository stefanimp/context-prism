# Contributing

Thanks for your interest in improving Context Prism.

## Local Setup

```bash
npm install
npm run dev
```

Use a development vault when testing plugin behavior.

## Pull Requests

- Keep changes focused.
- Add tests for ranking or normalization changes.
- Run `npm run build` and `npm test` before opening a pull request.
- Document user-facing behavior in `README.md`.
